
@using waterwork.DAL;
@using (Html.BeginForm("Createinvoiceperiods", "Water_usage"))
{
    @Html.DevExpress().FormLayout(settings =>
{
    settings.Name = "formLayout";
    settings.AlignItemCaptionsInAllGroups = true;
    settings.Items.AddEmptyItem();

    settings.SettingsAdaptivity.AdaptivityMode = FormLayoutAdaptivityMode.SingleColumnWindowLimit;
    settings.SettingsAdaptivity.SwitchToSingleColumnAtWindowInnerWidth = 800;
    settings.Items.AddGroupItem(g =>
    {
        g.Caption = "สร้างงวดการออกใบแจ้งหนี้";
        g.ColCount = 2;
        g.Items.Add(s =>
        {
            s.FieldName = "date";
            s.Caption = "วันที่";
            s.NestedExtensionType = FormLayoutNestedExtensionItemType.DateEdit;
            var set = s.NestedExtensionSettings as DateEditSettings;

        });


        g.Items.Add(i =>
        {
            i.ShowCaption = DefaultBoolean.False;
            i.HorizontalAlign = FormLayoutHorizontalAlign.Right;
            i.Width = 150;
            i.NestedExtension().Button(s =>
            {
                s.Name = "applyButton";
                s.Text = "ตกลง";
                s.UseSubmitBehavior = true;
            });
        });
        g.Items.Add(s =>
        {
            s.Caption = " ";
            s.NestedExtension().Label(i =>
            {
                i.Name = "label";
                i.Text = "สร้างงวดการออกใบแจ้งหนี้ในฐานข้อมูลเพื่อให้ผู้ป้อนข้อมูลน้ำประปาใช้อ้างอิงในการบันทึกข้อมูลการใช้น้ำประปา";
                i.ControlStyle.ForeColor = System.Drawing.Color.Red;
            });

        });
    });
}).GetHtml();

}

@Html.Partial("invoiceperiods",DALCreateinvoiceperiods.Getinvoiceperiods())